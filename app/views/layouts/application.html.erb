<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= stylesheet_link_tag "website", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/8aafeb2c84.js" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>

  </head>

  <body>
<div class="navbar d-flex justify-content-between align-items-center ">
  <div>
    <% if user_signed_in? && current_user.admin? %>
      <%= link_to "New Flat", new_flat_path, class: "fa-duotone fa-solid fa-plus me-3" %>
    <% end %>
    <p class="contact-info">  Do you have any questions? Call us! Mon - Fri 9:00-5:00</p>
  </div>

  <div>
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, data: { turbolinks: false }, class: "btn btn-outline-secondary" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light me-2" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

    <%= yield %>
  </body>
</html>
